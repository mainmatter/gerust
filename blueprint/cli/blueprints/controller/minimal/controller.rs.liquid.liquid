use crate::{error::Error, state::SharedAppState};
use axum::{extract::State, http::StatusCode};
use tracing::info;

#[axum::debug_handler]
pub async fn action(State(app_state): State<SharedAppState>) -> Result<StatusCode, Error> {
    todo!("implement!");

    info!("responding with {:?}", StatusCode::OK);

    Ok(StatusCode::OK)
}
